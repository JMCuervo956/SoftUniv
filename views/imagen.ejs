<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Insertar Imagen con JavaScript</title>
</head>
<body>
    <h1>Ejemplo de Inserción de Imagen</h1>
    <div id="image-container"></div> <!-- Contenedor donde se insertará la imagen -->

    <script>
        // Crear un nuevo elemento img
        const img = document.createElement('img');

        // Configurar el atributo src (ruta de la imagen)
        img.src = "imageninicial.jpeg"

        //img.src = 'ruta-de-tu-imagen.jpg'; // Cambia esta ruta a la ubicación de tu imagen

        // Opcional: Configurar atributos adicionales
        img.alt = 'Descripción de la imagen';
        img.width = 300; // Ancho de la imagen
        img.height = 200; // Alto de la imagen

        // Insertar la imagen en el contenedor
        const container = document.getElementById('image-container');
        container.appendChild(img);
    </script>
</body>
</html>
